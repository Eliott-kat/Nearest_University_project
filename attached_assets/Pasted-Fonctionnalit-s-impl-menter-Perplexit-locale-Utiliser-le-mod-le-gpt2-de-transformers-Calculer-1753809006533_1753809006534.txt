Fonctionnalités à implémenter :
Perplexité locale

Utiliser le modèle gpt2 de transformers

Calculer la perplexité moyenne du texte soumis

Perplexité basse (ex : < 40) = probable texte IA

Burstiness

Calculer l’écart-type du nombre de mots par phrase (via numpy)

Faible burstiness = texte probablement IA

Décision

Si perplexity < 50 et burstiness < 15 → considérer que c’est un texte généré par IA

Sinon → texte humain

📦 Dépendances à installer :
bash
Copier
Modifier
pip install transformers torch numpy
🗂️ Code à intégrer dans un fichier utils/ai_gptzero_like.py :
python
Copier
Modifier
from transformers import GPT2LMHeadModel, GPT2TokenizerFast
import torch
import numpy as np

def calculate_perplexity(text):
    model_name = 'gpt2'
    model = GPT2LMHeadModel.from_pretrained(model_name)
    tokenizer = GPT2TokenizerFast.from_pretrained(model_name)
    model.eval()

    encodings = tokenizer(text, return_tensors='pt')
    input_ids = encodings.input_ids

    with torch.no_grad():
        outputs = model(input_ids, labels=input_ids)
        loss = outputs.loss
    return torch.exp(loss).item()

def calculate_burstiness(text):
    sentences = text.split(".")
    lengths = [len(s.split()) for s in sentences if len(s.strip()) > 3]
    if len(lengths) < 2:
        return 0
    return np.std(lengths)

def detect_ai_gptzero_like(text, perplexity_thresh=50, burstiness_thresh=15):
    perplexity = calculate_perplexity(text)
    burstiness = calculate_burstiness(text)
    
    is_ai = perplexity < perplexity_thresh and burstiness < burstiness_thresh
    return {
        "is_ai": is_ai,
        "perplexity": perplexity,
        "burstiness": burstiness
    }
💬 Exemple d’appel dans mon app Flask :
python
Copier
Modifier
from utils.ai_gptzero_like import detect_ai_gptzero_like

resultat = detect_ai_gptzero_like(texte)

if resultat["is_ai"]:
    print("Texte généré par une IA détecté")
else:
    print("Texte probablement humain")
Merci de l’intégrer dans mon projet et de le connecter à la vue soumettre_document.